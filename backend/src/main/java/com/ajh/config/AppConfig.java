package com.ajh.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Removemos o método restTemplate() daqui 
    // pois ele já existe no IntegrationConfig.java
}